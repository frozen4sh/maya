{
    string $sel[] = `ls -type joint`;
    for ($s in $sel) {
        if (`objExists ($s +".drawStyle")`) setAttr ($s +".drawStyle") 0;
    }
}



{
    string $sel[] = `ls -type joint`;
    for ($s in $sel) {
        if (`objExists ($s +".intermediateObject")`) setAttr ($s +".intermediateObject") 0;
    }
}